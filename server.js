// jshint esversion:6
const express = require("express");

const app = express();  // Function that represents the Express module, and we bind that to the word app

app.listen(3000); // this tells it to listen on a specific port
